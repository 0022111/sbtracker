# Built outputs
/build/
/app/build/
*.apk
*.aab

# Gradle
.gradle/
.gradle.bak/
local.properties

# IDE
.idea/
*.iml
.DS_Store

# Kotlin
.kotlin/

# Agent working dirs (session-specific, not project code)
.claude/settings.json
.claude/settings.local.json
.claude/worktrees/
# .claude/commands/ is tracked — project slash commands belong in the repo
.cursor/
.gemini/

# Reference app (protocol reference only — do not ship)
\!gitignore.referenceonly-reactive-volcano-app-main/
